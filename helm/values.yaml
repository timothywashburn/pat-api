image:
  registry: kyrokrypt
  pullPolicy: Always
  server:
    tag: latest
  client:
    tag: latest

ingress:
  host: pat.timothyw.dev
  ingressClassName: traefik

namespace: pat

mongodb:
  storage:
    size: "2Gi"
    storageClass: "local-path"
  port: "30019"